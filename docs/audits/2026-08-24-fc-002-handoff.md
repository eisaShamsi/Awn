# تقرير مجلس التدقيق: الإلغاء الآمن للتشغيل — HANDOFF

- **التاريخ:** `2026-08-24`
- **توقيت الإصدار:** `2026-08-24T09:56:48+04:00`
- **مفهوم الوظيفة:** `FC-002 v0.3`
- **حالة اعتماد المفهوم:** `APPROVED`
- **بصمة مفهوم الوظيفة:** `b3431cb639070949deb06286497240ebfdf350c651795841b524a233c3fb2bdf`
- **مرجع اعتماد المالك:** رسالة المالك «أعتمد FC-002 v0.3»؛ `MOCH-2026-08-24-001`، المادة 1
- **المرحلة:** `HANDOFF`
- **نطاق التغيير:** تسليم وظيفة الإلغاء الآمن للتشغيل للتجربة المحلية بعد التحقق من الحقيقة، والاستعادة، وحدود الأثر، وتجربة الاستخدام، وسلامة الأدلة.
- **لقطة التنفيذ:** commit `7ba697629b2f63a8b6143dfaef913af965dd7799`؛ [بيان BUILD](2026-08-24-fc-002-build.manifest.sha256) من 48 ملفًا ببصمة خام `58d7375649b16201b387e9af0f48bdefac41066a5fa2362a4cd27eeb58c1b94f` ومطابقة `48/48`؛ [تقرير BUILD](2026-08-24-fc-002-build.md) ببصمة `b6bc4813fec840bb0c9137671431b6f461de3e888e18137ca032bae8b5153510`.
- **بوابة التكامل المستمر:** [GitHub Actions run 32694671466](https://github.com/eisaShamsi/Awn/actions/runs/32694671466) — `SUCCESS` على لقطة التنفيذ نفسها.
- **قرار المجلس:** `READY_FOR_OWNER_TRIAL`

## الأدلة المشتركة

- **المفهوم والتصميم:** [FC-002 v0.3](../concepts/FC-002-safe-run-cancellation.md)، [ADR-013](../adr/ADR-013-durable-run-cancellation.md)، و[تصميم الوظيفة](../design/FC-002-safe-run-cancellation-design.md).
- **تثبيت اللقطة:** تطابقت `main` و`HEAD` و`origin/main` مع commit التنفيذ عند المراجعة؛ لم يوجد drift متعقب، وبقي `.obsidian/` محليًا وغير متعقب وخارج النطاق.
- **CI/Python:** نجح إنشاء PostgreSQL والترحيل وRuff وفحص التنسيق والاختبارات؛ النتيجة `72 passed` في قاعدة نظيفة.
- **CI/Web:** نجح تدقيق الحزم وESLint وTypeScript وبناء Next.js؛ النتيجة `0 vulnerabilities`.
- **اختبارات الواجهة:** أُعيد Vitest محليًا على commit التنفيذ ونجح `10/10`. لا تشغله مهمة CI الحالية؛ سُجل ذلك تحسينًا غير حاجب.
- **الترحيلات:** نجحت الترقية إلى `0006` والرجوع `0006 → 0005` ثم الترقية مجددًا على PostgreSQL. الرجوع المسموح يحذف تاريخ الإلغاء، ولذلك لا يُستخدم بديلًا عن استعادة نسخة احتياطية عند أهمية البيانات.
- **الفحص الحي:** نُفذ إلغاء قبل الأثر على API والعامل والواجهة الحاليين، وظهرت النتيجة والخطوة الآمنة والخط الزمني على `1280×720` و`375×812` بخط Calibri ومن دون تجاوز أفقي.
- **الدليل المرئي:** راجع Art Director وUI/UX Auditor الصورتين المؤقتتين بعد مطابقة بصمتيهما. بعد اكتمال المراجعة حُذفتا نهائيًا من `Temp`، ثم تحقق Safety Inspector من غياب الاسمين ومن عدم وجود أي تطابق للبصمتين بين 19 ملف PNG متبقيًا.
- **أثر الاختبار:** حُذفت المحادثة الاصطناعية وحدها مع تشغيل واحد ورسالتين بعد الفحص، وأُوقفت خدمتا الاختبار على `8100` و`3100`. لم تُحذف بيانات المالك أو ملفات المشروع.
- **السرية:** لا يوجد ملف أسرار متعقب، و`.env` مستبعد، وفحص الأنماط الحساسة نظيف.
- **دليل الاستخدام:** يشرح [دليل المستخدم](../USER_GUIDE.md) تشغيل PostgreSQL وAPI والعامل والواجهة، واختيار التشغيل الصحيح، وطلب الإلغاء، وقراءة النتيجة أو عدم اليقين والخطوة الآمنة.

## Art Director

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `art_director_fc002_handoff`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل الملفات ولم يحذف الدليل قبل إتمام مراجعته.
- **المدخلات الدقيقة التي راجعها:** commit التنفيذ والبيان والتقرير وCI، وصورتا العرض وبصمتاهما.
- **مطابقة المفهوم:** يقدّم التسلسل الحالة ومعناها والخطوة الآمنة ثم الزمن والدليل، ويعبّر بصريًا عن أن الإلغاء فرامل لا ممحاة.
- **الأدلة:** RTL وCalibri والمحتوى العربي/LTR والعرضان متماسكة؛ الحالات تستخدم النص والرمز واللون، ونسب التباين الأساسية بين `4.92:1` و`15.22:1`.
- **الملاحظات:** لا توجد ملاحظة مفتوحة؛ أوصى صراحة بـ`READY_FOR_OWNER_TRIAL`.

## UI/UX Auditor

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `uiux_fc002_build_recheck`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** commit التنفيذ والبيان والتقرير، دليل المستخدم، رحلة الإلغاء، الاستعادة، الصور، وإتاحة الاستخدام.
- **مطابقة المفهوم:** تميز الواجهة بين إرسال الطلب وقبوله والإلغاء قبل الأثر والأثر الجزئي أو الكامل وعدم اليقين والتعارض والفشل، ولا تعيد تنفيذ أثر غير مؤكد.
- **الأدلة:** اختيار التشغيل، واستعادة POST المفقود، وحراسة السياق، والخطوة الآمنة، وRTL وCalibri والعرض الضيق و`aria-live`.
- **الملاحظات:** لا توجد ملاحظة مفتوحة؛ أوصى صراحة بـ`READY_FOR_OWNER_TRIAL`.

## Coding Inspector

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `coding_inspector`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** commit التنفيذ والبيان `48/48` والتقرير وCI، والترحيل والتعافي والاختبارات والحدود الموثقة.
- **مطابقة المفهوم:** التنفيذ يثبت أوامر الإلغاء وسلطة العامل وحد الأثر والدليل، ويصالح الحالات بعد إعادة التشغيل من دون إعادة الأثر.
- **الأدلة:** CI ناجح على commit نفسه، `72 passed` في PostgreSQL نظيف، وVitest محلي `10/10`، مع نجاح الترحيلات وRuff والويب.
- **الملاحظات:** لا توجد ملاحظة مفتوحة؛ أوصى صراحة بـ`READY_FOR_OWNER_TRIAL`. إضافة Vitest إلى CI تحسين لاحق غير حاجب.

## Safety Inspector

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `safety_inspector`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل الملفات، وطلب إغلاق أثر الدليل المؤقت ثم أعاد التحقق منه.
- **المدخلات الدقيقة التي راجعها:** commit التنفيذ والبيان والتقرير وCI، والأسرار والبيانات والرجوع وحدود الحقيقة وتنظيف آثار الاختبار.
- **مطابقة المفهوم:** لا تُمنح حالة إلغاء مؤكدة لأثر مجهول، ولا يمثل الرجوع أو غياب المورد محوًا للأثر أو دليلًا قاطعًا على عدمه.
- **الأدلة:** نجاح CI، ونظافة فحص الأسرار، وتوقف المنافذ المؤقتة، وغياب الصورتين وبصمتيهما بعد الحذف.
- **الملاحظات:** أُغلقت `SAF-HOF-001` بعد تنظيف الدليل المؤقت؛ لا توجد ملاحظة مفتوحة، وأوصى صراحة بـ`READY_FOR_OWNER_TRIAL`.

## سجل الملاحظات

| المعرّف | العضو | الدرجة | بند المفهوم | الدليل | الإجراء المطلوب | الحالة |
|---|---|---|---|---|---|---|
| `SAF-HOF-001` | Safety | MEDIUM | حماية بيانات المالك في أدلة التسليم | بقاء صورتين مؤقتتين بعد الفحص الحي | مراجعتهما ثم حذفهما والتحقق بالاسم والبصمة | `RESOLVED` |

## المخاطر والحدود المتبقية

| الخطر أو الحد | الدرجة | سبب بقائه | المراقبة أو التخفيف |
|---|---|---|---|
| قد يبقى أثر خارجي بلا دليل حصري في حالة `uncertain` | MEDIUM | حد في الموارد الخارجية | لا إعادة تلقائية؛ فحص المورد أو تدخل يدوي وفق الخطوة الآمنة |
| لا يوجد تعويض تلقائي لأثر جزئي أو مكتمل | MEDIUM | خارج مفهوم `FC-002 v0.3` | تعرض الواجهة الحقيقة ولا تدعي محو الأثر |
| `alembic downgrade` المسموح لا يحفظ تاريخ الإلغاء | MEDIUM | مخطط `0005` لا يمثل بيانات FC-002 | نسخة PostgreSQL محمية قبل بيانات مهمة؛ إيقاف API والعامل؛ تفضيل استعادة النسخة الاحتياطية |
| Vitest غير مشغل في GitHub CI حاليًا | LOW | سير العمل السابق لا يتضمن `npm test` | نجح `10/10` محليًا على commit التنفيذ؛ يُضاف إلى CI في تحسين مستقل |
| التسليم محلي لمستخدم واحد وليس نشرًا إنتاجيًا | LOW | حدود المرحلة الحالية | التجربة محليًا وببيانات غير شديدة الحساسية فقط |

## إعادة التحقق

- راجع الأعضاء الأربعة commit التنفيذ نفسه، وبيان BUILD نفسه، ونتيجة CI نفسها، ولم يعدّل أي منهم الملفات.
- أعاد UI/UX وCoding التحقق من الرحلة والاختبارات والحدود، وأعاد Art Director فحص العرضين النهائيين.
- أصدر Safety Inspector أولًا `PASS_WITH_CONDITIONS` بسبب الدليل المرئي المؤقت، ثم أغلق `SAF-HOF-001` وأصدر `PASS` بعد التحقق من الحذف بالاسم والبصمة.
- انتهت الأحكام المستقلة الأربعة إلى `PASS` ولا توجد ملاحظة أو شرط مفتوح.

## تجربة المالك المقترحة

1. أخذ نسخة PostgreSQL إذا كانت قاعدة التجربة تحتوي بيانات مهمة، ثم تشغيل `alembic upgrade head`.
2. إعادة تشغيل API والعامل من الشيفرة الحالية، ثم تشغيل واجهة الويب؛ لا تكفي خدمة قديمة ما زالت تستمع إلى المنفذ.
3. إنشاء طلب ينتج تشغيل أداة، واختيار التشغيل المقصود بالمعرف والحالة، ثم الضغط على «طلب الإلغاء».
4. متابعة بطاقة التشغيل حتى نتيجة نهائية أو `uncertain`، واتباع «الخطوة الآمنة التالية» من دون إعادة التنفيذ عند غياب اليقين.

## خلاصة منسق التطوير

المفهوم معتمد، والأدوار الأربعة أصدرت `PASS` على لقطة التنفيذ، ونجحت بوابة CI، وأُغلقت ملاحظة الدليل المؤقت، ولا توجد finding مفتوحة. لذلك تطبق قواعد المجلس قرار `READY_FOR_OWNER_TRIAL`: يجوز تقديم `FC-002 v0.3` للمالك للتجربة المحلية. هذا القرار لا يمثل قبول المالك النهائي للوظيفة.
