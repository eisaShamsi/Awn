# تقرير مجلس التدقيق: الإلغاء الآمن للتشغيل — BUILD

- **التاريخ:** `2026-08-24`
- **توقيت الإصدار:** `2026-08-24T09:35:34+04:00`
- **مفهوم الوظيفة:** `FC-002 v0.3`
- **حالة اعتماد المفهوم:** `APPROVED`
- **بصمة مفهوم الوظيفة:** `b3431cb639070949deb06286497240ebfdf350c651795841b524a233c3fb2bdf`
- **مرجع اعتماد المالك:** رسالة المالك «أعتمد FC-002 v0.3»؛ `MOCH-2026-08-24-001`، المادة 1
- **المرحلة:** `BUILD`
- **نطاق التغيير:** أمر إلغاء دائم، وحد أثر وسلطة عامل، ومصالحة دليل متأخر أو متعارض، وAPI مقيدة، وتجربة عربية متعددة التشغيلات مع استعادة ووضوح النتيجة.
- **لقطة التغيير:** [بيان BUILD](2026-08-24-fc-002-build.manifest.sha256)، 48 ملفًا، SHA-256 خام `4a12ba08bd05ba624ff3271b508af319135a8850179a6bddb923469e95c3d44d`، ومطابقة `48/48` لدى الأعضاء الأربعة.
- **قرار المجلس:** `READY_FOR_NEXT_PHASE`

## الأدلة المشتركة

- **المفهوم والتصميم:** [FC-002 v0.3](../concepts/FC-002-safe-run-cancellation.md)، [ADR-013](../adr/ADR-013-durable-run-cancellation.md)، و[تصميم الوظيفة وخطة تحققها](../design/FC-002-safe-run-cancellation-design.md).
- **فرق الشيفرة:** المجال والمستودعات والترحيل `20260824_0006` وخدمة الإلغاء والعامل وفاحصا أثر المهمة والملف وAPI وBFF ولوحة التحكم والوثائق والاختبارات، محصورة في البيان أعلاه.
- **Python/PostgreSQL:** `71 passed, 1 skipped`؛ التخطي الوحيد يحمي المستخدم الموجود في قاعدة التكامل المحلية. أُعيدت اختبارات سباقات PostgreSQL الثلاثة خمس مرات سابقًا ونجحت `15/15`.
- **الويب:** Vitest `10/10`، وTypeScript وESLint وبناء Next.js `16.3.2` ناجحة.
- **جودة وأمن التبعيات:** Ruff ناجح، و`npm audit --audit-level=low` أعاد `0 vulnerabilities`، وفحص الأنماط السرية لم يجد نتيجة.
- **الترحيلات:** ترقية `0006`، والرجوع `0006 → 0005` ثم الترقية مجددًا، وفحص عدم وجود عمليات ترحيل معلقة، نجحت على PostgreSQL المحلي.
- **الفحص الحي:** نُفذ الإلغاء قبل الأثر على API والنواة الحاليين، وانتقل التشغيل إلى `cancelled` وظهرت بطاقة «أُلغي قبل الأثر» والخطوة الآمنة والخط الزمني.
- **الدليل المرئي:** سطح مكتب `1280×720` ببصمة `2efd63f72d78b90a2aa690940d59b3742b6410b6f5183775b40acccb421badc3`، وعرض `375×812` ببصمة `aa83c73c13ee6a18571690a1d8160e9ed9f45a9b2980adc246082e63b58c8322`. لم تُحفظ الصورتان في المستودع لأنهما التقطتا بيانات المالك المحلية؛ راجعهما Art Director وUI/UX Auditor من ملفات مؤقتة فقط.
- **قياسات المتصفح:** Calibri أول خط محسوب؛ عرض الصفحة `1265` ضمن viewport `1280`، و`360` ضمن viewport `375`؛ بطاقة الإلغاء الضيقة بقيت بين `30.67` و`329.33` بكسل.
- **الأثر الجانبي للاختبار:** أُنشئت محادثة اصطناعية واحدة للتدقيق الحي ثم حُذفت وحدها مع تشغيل واحد ورسالتين؛ الحذف غير قابل للاستعادة ولا يمس بيانات المالك الأخرى. أُوقفت خدمتا التدقيق المؤقتتان على المنفذين `8100` و`3100`.
- **فحوص لم تُشغل:** لا يوجد فحص مطلوب غير مشغل في بوابة BUILD.
- **ملاحظة تنسيق:** `git diff --check` ناجح؛ ظهرت تحذيرات Windows عن LF/CRLF فقط.

## Art Director

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `art_director_fc002_build_final`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** بيان BUILD وبصمته، المفهوم والتصميم، CSS ومكوّن اللوحة والاختبارات، وصورتا المتصفح الحي وبصمتاهما وقياسات DOM.
- **مطابقة المفهوم:** التعبير البصري يوضح أن الإلغاء فرامل لا ممحاة؛ لا يعرض توقفًا قبل الأثر بوصفه نجاح أثر.
- **الأدلة:** هرمية النتيجة والمعنى والخطوة الآمنة والخط الزمني؛ ألوان ورموز مستقلة عن اللون وحده؛ RTL ومحتوى مختلط وCalibri وعرض ضيق بلا تجاوز.
- **الملاحظات:** `FC002-B-ART-004` أُغلقت بالفحص الحي على العرضين؛ لا توجد ملاحظة مفتوحة.

## UI/UX Auditor

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `uiux_fc002_build_recheck`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** البيان والمفهوم، لوحة التحكم واختباراتها، حالات الإلغاء والتعارض، حراس السياق، وصور وقياسات العرضين.
- **مطابقة المفهوم:** يمكن طلب الإلغاء ومتابعة نتيجته وتشغيلاته المتعددة دون ادعاء زائف أو خلط محادثات.
- **الأدلة:** استعادة POST المفقود، وحراسة ردود التفاصيل والإلغاء والإنشاء والإرسال المتأخرة، والتركيز، وتسميات التشغيل، و`aria-live`، والفحص الحي.
- **الملاحظات:** `BUILD-UX-CONTEXT-001` و`BUILD-UX-AUTHORITY-001` و`BUILD-UX-CANCEL-001` و`BUILD-UX-RESPONSIVE-001` و`BUILD-UX-A11Y-001` كلها `RESOLVED`؛ لا توجد ملاحظة مفتوحة.

## Coding Inspector

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `coding_inspector`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** جميع ملفات البيان، فرق الشيفرة، الترحيل، العقود والمستودعات والخدمة والعامل وBFF والواجهة والاختبارات ونتائج البناء.
- **مطابقة المفهوم:** `AC-001` إلى `AC-008` مرتبطة بمسارات تنفيذ واختبارات فعلية، مع قفل وسلطة ودليل ونتيجة نهائية صادقة.
- **الأدلة:** ترتيب أقفال `run → calls → cancellation → steps`، ونقطة الالتزام وهوية العامل، ومصالحة الإنتاج، وعقود الدليل المغلقة، والاختبارات السلبية وPostgreSQL.
- **الملاحظات:** `FND-COD-B09` و`FND-COD-B10` و`FND-COD-B11` كلها `RESOLVED`؛ لا توجد ملاحظة مفتوحة.

## Safety Inspector

- **الحكم:** `PASS`
- **هوية المدقق/الوكيل:** `safety_inspector`
- **دليل الاستقلال عن المنفذ:** وكيل مستقل بوضع قراءة؛ لم يعدّل أي ملف.
- **المدخلات الدقيقة التي راجعها:** البيان والمفهوم ونموذج التهديد، حدود المالك والمساحة، سلطة العامل والدليل، الاستعادة والسباقات، وحراس سياق الواجهة والاختبارات.
- **مطابقة المفهوم:** لا يمنح النظام `cancelled` عند أثر مجهول، ولا يسمح لدليل عامل أو رمز أو مصدر غير موثوق بتقرير الحقيقة، ولا يخلط تشغيلًا بين محادثتين.
- **الأدلة:** POST بلا جسم ومقيد بالنطاق، تحقق schema/source/token/worker، إيصال ملف دائم، غياب غير حصري يبقى `unknown`، وحراس كل استجابة غير متزامنة.
- **الملاحظات:** `SAF-BLD-003` إلى `SAF-BLD-007` كلها `RESOLVED`؛ لا توجد ملاحظة مفتوحة.

## سجل الملاحظات

| المعرّف | العضو | الدرجة | بند المفهوم | الدليل | الإجراء المطلوب | الحالة |
|---|---|---|---|---|---|---|
| `BUILD-R1-CAUSALITY` | Coding + Safety | HIGH | `AC-002..006` | أوقات قبل الأقفال، وترتيب قفل غير صريح، وعقد دليل مفتوح | وقت سببي بعد الأقفال، ترتيب موحد، وأكواد ومخططات ومصادر مغلقة | `RESOLVED` |
| `BUILD-R1-UI-RACES` | UI/UX + Coding | HIGH | `AC-007` | ردود تشغيل وإلغاء متداخلة تغير التفاصيل أو التركيز | حالة حسب `run_id` وحراس نية ونسخة الطلب | `RESOLVED` |
| `BUILD-R1-VISUAL` | Art + UI/UX | MEDIUM | `AC-007` | ألوان وأحجام وحالات لا تميز المعنى بما يكفي | حالات ورموز ونصوص وأحجام متمايزة وCalibri | `RESOLVED` |
| `BUILD-R2-RECONCILIATION` | Coding + Safety | HIGH | `AC-004..006` | المصالحة غير موصولة بالإنتاج وغياب المورد قد يفسر «لا أثر» | فاحص إنتاج غير مؤثر، وإيصال دائم، و`unknown` للغياب غير الحصري | `RESOLVED` |
| `BUILD-R2-AUTHORITY` | Coding + Safety | HIGH | `AC-004..006` | دليل دون هوية عامل الالتزام أو توقيت رصد سببي | `effect_commit_worker_id` والتحقق من العامل والرمز والمصدر وUTC | `RESOLVED` |
| `FND-COD-B09` / `SAF-BLD-006` | Coding + Safety | HIGH | `AC-002,006,007` | إنشاء المحادثة لم يبدأ سياق عرض جديدًا | حراسة POST و`beginViewContext` قبل التحميل واختبار الرد المتأخر | `RESOLVED` |
| `FND-COD-B10` / `SAF-BLD-007` | Coding + Safety | HIGH | `AC-001,006..008` | رد رسالة أو تشغيل متأخر يلوث محادثة أحدث | حراسة كل تحديث بعد `await` والاستعادة من GET والتحقق الدفاعي للهوية | `RESOLVED` |
| `FC002-B-ART-004` | Art | MEDIUM | `AC-007` | غياب فحص متصفح حي لسطح المكتب والعرض الضيق | فحص حي وصور وقياسات DOM وإصلاح الفائض الأفقي | `RESOLVED` |

## المخاطر المتبقية

| الخطر | الدرجة | سبب عدم المعالجة | قرار المالك إن لزم | المراقبة أو التخفيف |
|---|---|---|---|---|
| قد يبقى أثر خارجي لا يوفر دليلًا حصريًا في حالة `uncertain` | MEDIUM | حد جوهري في الموارد الخارجية وليس خطأ تصنيف | لا يلزم للانتقال؛ يظهر للمستخدم بوضوح | لا إعادة تلقائية؛ فحص المورد أو تدخل يدوي |
| لا يوجد تعويض تلقائي لأثر جزئي أو مكتمل | MEDIUM | خارج نطاق `FC-002 v0.3` المعتمد | لا يلزم للانتقال | تعرض الواجهة الخطوة الآمنة وتمنع الادعاء بالإلغاء |
| الفحص الحي غطى حالة الإلغاء قبل الأثر، بينما الحالات الأخرى اعتمدت الاختبار والشفرة | LOW | إنشاء كل حقيقة خارجية حيًا غير متناسب مع BUILD المحلي | لا يلزم | تعاد مراجعتها في اختبارات التكامل والتسليم عند توفر مورد مماثل |

## إعادة التحقق

- أُعيد تجميد البيان بعد كل معالجة؛ القرار يخص البصمة الخام `4a12ba08bd05ba624ff3271b508af319135a8850179a6bddb923469e95c3d44d` فقط.
- أعاد الأعضاء الأربعة التحقق من `48/48` دون تعديل الملفات.
- أُعيدت اختبارات Python/PostgreSQL والويب والأنواع وlint والبناء وRuff وتدقيق الحزم، وأُجري فحص متصفح حي على عرضين.
- أغلق كل عضو ملاحظاته ضمن اختصاصه، وانتهت الأحكام الأربعة إلى `PASS`.

## خلاصة منسق التطوير

المفهوم معتمد، وكل دور منطبق أصدر `PASS`، ولا توجد ملاحظة أو شرط مفتوح، وكل الأدلة المطلوبة ناجحة على اللقطة المحددة. لذلك تطبق القواعد قرار `READY_FOR_NEXT_PHASE`: يجوز الانتقال من BUILD إلى تثبيت التغيير والتحقق من CI ثم مرحلة HANDOFF، ولا يعني هذا بعدُ أن البناء جاهز لتجربة المالك.
